# Homebrew Tap

Homebrew tap for Scrubadub.

## Install

```bash
brew install --cask steven-haddix/tap/scrubadub
```

The `scrubadub` cask is published by the Scrubadub release workflow after a GitHub release is created.
